{% extends 'base.html.twig' %}

{% block title %}Valider ma commande - Le Distributeur{% endblock %}

{% block body %}
<div class="container mt-5 mb-5">
    <h2>Je passe ma commande</h2>
    <p>Choisissez vos préférences avant de passer votre commande sur le Distributeur.</p>
    <hr>

    <div class="row">
        <div class="col-md-6">
            {% set formHtml %}
                {{ form_start(form, {action:path('app_order_recap')}) }}
                {{ form_end(form) }}
            {% endset %}

                {{ formHtml|replace({'[br]' : '<br/>'})|raw }}
            
        </div>
        <div class="col-md-6">
            <div class="text-center"> 
                <p>Retrouvez le récapitulatif de vos produits</p>
            </div> 
            {% for key,product in cart %}
            <div class="row {% if key > 0  %}mt-2{% endif %}">
                <div class="col">
                    <img src="/images/product/{{ product.product.image }}" alt="{{ product.product.titreArticle }}" height="155px">
                </div>
                <div class="col my-auto">
                {{ product.product.titreArticle }} <br>
                </div>
                <div class="col my-auto">
                    x {{ product.quantity }}
                </div>
            </div>
                
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}
